<% @questions.each do |question| %>
  <a href="/questions/<%= question.id %>"><%= question.title %></a>
  <%= question.content %>
  <%= link_to "upvote", upvote_path(question), method: :post %>
  <%= link_to "downvote", downvote_path(question), method: :post %>
  <br>
<% end %>

<%= render partial: 'new' %>
