<% if @answer %>
  <%= form_for [@question, @answer] do |f| %>

  <p>
    <%= f.label :answer %><br>
    <%= f.text_area :content %>
  </p>

  <p>
    <%= f.submit 'Submit' %>
  </p>
  <% end %>
<% end %>
